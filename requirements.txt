# Minimal requirements for a typical Python agent/web project
# Adjust versions or add/remove packages to match your project's needs.
# Recommended Python version: 3.10+

fastapi>=0.95.0
uvicorn[standard]>=0.21.0
httpx>=0.24.0
aiohttp>=3.8.4
pydantic>=1.10.11,<2
requests>=2.31.0
python-dotenv>=1.0.0
typing-extensions>=4.8.0

# Testing / development
pytest>=7.4.0
pytest-asyncio>=0.21.0

# Formatting / linting (dev)
black>=23.9.1
isort>=5.12.0
flake8>=6.1.0
mypy>=1.5.1

# If you depend on a specific SDK (e.g. Strands SDK), add it here:
strands-sdk>=0.1.0